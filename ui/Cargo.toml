[package]
name = "voxbox-ui"
version = "0.1.0"
edition = "2021"
license = "MIT"
description = "Shared VoxBox desktop UI layer for iced."

[dependencies]
iced = { version = "0.10", features = ["canvas"] }
