[workspace]
members = [
    "core",
    "cli",
    "ui",
    "wasm",
    "desktop",
    "audio-lab-mcp",
    "plugin",
    "plugin-ui",
    "xtask",
]
resolver = "2"

[profile.release]
lto = "thin"
strip = "symbols"
