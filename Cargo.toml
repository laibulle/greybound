[workspace]
members = [
    "core",
    "cli",
    "ui",
    "wasm",
    "desktop",
    "lab-core",
    "lab-desktop",
    "audio-lab-mcp",
    "plugin",
    "plugin-ui",
    "xtask",
]
resolver = "2"

[profile.release]
lto = "thin"
strip = "symbols"
