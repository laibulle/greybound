# Desktop Resources

Files in this directory are copied into:

`Greybound Free.app/Contents/Resources`

Use it for factory presets, bundled lightweight models, bundled IRs, images, and
other read-only assets that should ship with the desktop app. User-created or
downloaded content should live under the platform application support directory
at runtime, not inside the app bundle.
